#include <stdio.h>

/* NOTE: The arguments to main are the command line arguments. In this case, we
 *       don't actually need them, so we can list the parameters as "void"
 *       instead to reassure the compiler that we're not just ignoring data. */
int main(void) {
    printf("Hello, world!\n");

    return 0;
}
