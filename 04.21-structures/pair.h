/* NOTE: To define a type "struct Pair" composing two integers: */
struct Pair {
    int first;
    int second;
};

/* NOTE: To define "Pair" as a new name for "struct Pair": */
typedef struct Pair Pair;
